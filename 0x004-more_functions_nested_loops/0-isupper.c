/**
 * _isupper - Return 1 if character is uppercase else
 * zero
 * @c: first and only argument
 * Return: Always integer value
 */

int _isupper(int c)
{
	int isupper(int c);

	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
