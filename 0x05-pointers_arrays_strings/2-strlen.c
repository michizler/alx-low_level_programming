#include <stddef.h>
#include <string.h>

/**
 * _strlen - calculates the function of a string
 * @s: character argument/string
 * Return: Always integer value
 */

int _strlen(char *s)
{
	int ret = strlen(s);

	return (ret);
}
