#include <stdio.h>
#include <stdlib.h>

/* Description: Printing lowercase letters of the alphabet using putchar() */

/**
 * main - Entry point
 *
 * Return: Always 0 (Sucess)
 */

int main(void)
{
	int lower_x = tolower(x);

	for (int x = 'A'; x <= 'Z'; x++)
		putchar(lower_x);

	return (0);

}
