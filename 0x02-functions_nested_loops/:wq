/**
 * _islower - Takes int c as parameter and returns type integer
 * @c: ascii character
 * Description: checks for lowercase characters
 *
 */

int _islower(int c)
{
	int islower(int c);

	if (islower(c))
		return (1);
	else
		return (0);
}
